<Window x:Class="TravianFuryBoarClient.Views.LoginToolsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TravianFuryBoarClient.Views"
        mc:Ignorable="d"
        Title="LoginToolsView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="86*"></RowDefinition>
            <RowDefinition Height="136*"></RowDefinition>
            <RowDefinition Height="197*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150*"></ColumnDefinition>
            <ColumnDefinition Width="479*"></ColumnDefinition>
            <ColumnDefinition Width="163*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBox Text="{Binding Path = Username}" Width="200" Height="40" Grid.Row="1" Grid.Column="1" Margin="10 10 10 20" VerticalAlignment="Top"></TextBox>
        <TextBox Text="{Binding Path = Password}" Width="200" Height="40" Grid.Row="1" Grid.Column="1" Margin="10 10 10 20" VerticalAlignment="Bottom"></TextBox>
        <Button Command="{Binding Login}" Content="login" Grid.Row="2" Grid.Column="1" Width="200" Height="60"></Button>
    </Grid>
</Window>
